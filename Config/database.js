// Placeholder for database configuration. The user will provide models and middlewares.
// Export a minimal object to prevent runtime errors when imported.

module.exports = {
  connect: async () => {
    return Promise.resolve();
  }
};


